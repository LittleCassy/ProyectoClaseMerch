package com.example.ejercicioclase.data.collections

import com.example.ejercicioclase.data.Match

object Matches {
    val matchesList = listOf<Match>(
        Match("Liga Pokemon", "Ash", "Brock", "13:00")
    )
}